[project]
name = "ontic-wiki"
version = "0.1.0"
description = "Autonomous study wiki with nightly agent-driven reindexing."
requires-python = ">=3.12"
dependencies = [
  "pypdf>=6.9.0",
  "requests>=2.32.0",
]

[tool.uv]
package = false
